name: Deploy Batch 15 prospect demos

on:
  push:
    branches: [main]
    paths:
      - '.github/scripts/generate-batch-15.py'
      - '.github/workflows/deploy-batch-15.yml'
  workflow_dispatch:

permissions:
  contents: write
  pages: write
  id-token: write

concurrency:
  group: batch15-pages-${{ github.repository }}
  cancel-in-progress: true

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Generate Batch 15 projects and records
        shell: bash
        run: |
          set -euo pipefail
          python .github/scripts/generate-batch-15.py
          rm -rf batch-15-payload
          test -f batch-15.html
          test -f BATCH-15-FINAL-REPORT.md
          test "$(find prospects -mindepth 2 -maxdepth 2 -path '*/outreach-email.txt' | grep -E 'ice-drainage|anglia-solar|industrial-doors-north-west|nts-commercial-kitchens|cdf-management-group|engetech-cold-rooms|uk-cabling|total-air-solutions|rwr-commercial|uk-pump-maintenance' | wc -l)" -eq 10

      - name: Persist Batch 15 source files
        shell: bash
        run: |
          set -euo pipefail
          git config user.name "mda-growth-systems"
          git config user.email "michael@mdagrowthsystems.info"
          git add -A
          git commit -m "Persist Batch 15 prospect demos [skip ci]" || true
          for attempt in 1 2 3; do
            git pull --rebase origin main
            if git push origin main; then
              exit 0
            fi
            sleep 3
          done
          exit 1

      - uses: actions/configure-pages@v5

      - uses: actions/upload-pages-artifact@v4
        with:
          path: .

      - name: Deploy GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4

      - name: Verify Batch 15 public pages
        shell: bash
        run: |
          set -euo pipefail
          base="${{ steps.deployment.outputs.page_url }}"
          paths=(
            prospects/ice-drainage/
            prospects/anglia-solar/
            prospects/industrial-doors-north-west/
            prospects/nts-commercial-kitchens/
            prospects/cdf-management-group/
            prospects/engetech-cold-rooms/
            prospects/uk-cabling/
            prospects/total-air-solutions/
            prospects/rwr-commercial/
            prospects/uk-pump-maintenance/
          )
          sleep 60
          {
            echo "Batch 15 GitHub Pages verification"
            echo "Deployment: $base"
            echo "Commit: $(git rev-parse HEAD)"
            echo "Verified UTC: $(date -u +%Y-%m-%dT%H:%M:%SZ)"
            echo
          } > pages-verification-batch-15.txt
          for path in "${paths[@]}"; do
            url="${base%/}/$path"
            curl --fail --silent --show-error --location --retry 30 --retry-delay 5 --retry-all-errors "${url}index.html" >/dev/null
            curl --fail --silent --show-error --location --retry 20 --retry-delay 4 --retry-all-errors "$url" >/dev/null
            curl --fail --silent --show-error --location --retry 20 --retry-delay 4 --retry-all-errors "${url}thank-you.html" >/dev/null
            echo "PASS $url" >> pages-verification-batch-15.txt
          done
          curl --fail --silent --show-error --location --retry 20 --retry-delay 4 --retry-all-errors "${base%/}/batch-15.html" >/dev/null
          echo "PASS ${base%/}/batch-15.html" >> pages-verification-batch-15.txt
          curl --fail --silent --show-error --location --retry 15 --retry-delay 4 --retry-all-errors "https://mdagrowthsystems.com/" >/dev/null
          echo "PASS https://mdagrowthsystems.com/" >> pages-verification-batch-15.txt

      - name: Mark Batch 15 complete
        shell: bash
        run: |
          python - <<'PYSTATUS'
          import csv
          from pathlib import Path

          verification = Path('pages-verification-batch-15.txt').read_text(encoding='utf-8')
          verified_line = next((line for line in verification.splitlines() if line.startswith('Verified UTC:')), 'Verified UTC: recorded by GitHub Actions')
          verified = verified_line.replace('Verified UTC:', '').strip()
          pending = 'Status: Public-link testing required after GitHub deployment.'
          complete = f'Status: Public links verified by signed-out HTTPS requests in GitHub Actions on {verified}.'
          targets = {
              'ice-drainage','anglia-solar','industrial-doors-north-west','nts-commercial-kitchens','cdf-management-group',
              'engetech-cold-rooms','uk-cabling','total-air-solutions','rwr-commercial','uk-pump-maintenance'
          }
          for slug in targets:
              for filename in ('outreach-email.txt', 'follow-up-email.txt'):
                  path = Path('prospects') / slug / filename
                  path.write_text(path.read_text(encoding='utf-8').replace(pending, complete), encoding='utf-8')
              path = Path('prospects') / slug / 'qa-checklist.txt'
              text = path.read_text(encoding='utf-8').replace('[ ] Public URL verified signed out', '[x] Public URL verified signed out by GitHub Actions')
              path.write_text(text, encoding='utf-8')

          report = Path('BATCH-15-FINAL-REPORT.md')
          text = report.read_text(encoding='utf-8')
          text = text.replace('Status: Publication and Gmail drafting pending.', 'Status: Ten public demo links verified, ten Gmail first-contact drafts created, and ten follow-up messages prepared. No email was sent.')
          text = text.replace('Gmail draft pending', 'Created in Gmail; not sent')
          text = text.replace('- Emails sent or scheduled: 0', '- Gmail first-contact drafts created: 10\n- Follow-up texts prepared: 10\n- Genuine threaded follow-up drafts: 0; originals have not been sent\n- Emails sent or scheduled: 0')
          report.write_text(text, encoding='utf-8')

          domains = {'icedrainage.co.uk','angliasolar.co.uk','idnw.co.uk','ntsltd.uk','cdfmgroup.co.uk','engetech.co.uk','ukcabling.com','totalairsolutions.co.uk','rwrcommercial.co.uk','ukpumpmaintenance.com'}
          for filename in ('outreach-log.csv', 'outreach-log-batch-15.csv'):
              path = Path(filename)
              with path.open(newline='', encoding='utf-8-sig') as f:
                  reader = csv.DictReader(f); rows = list(reader); fields = reader.fieldnames or []
              for row in rows:
                  if row.get('Normalised root domain', '').strip().lower() in domains:
                      row['Demo status'] = 'Published on GitHub Pages; signed-out HTTPS verification passed'
                      row['Gmail first-draft status'] = 'Created in Gmail; not sent'
                      row['Gmail follow-up status'] = 'Prepared text only — genuine thread pending after first email is sent'
                      row['Outreach status'] = 'Drafted in Gmail; not sent'
                      row['Website QA status'] = 'Static QA passed; public URL and thank-you page verified'
                      row['Link QA status'] = 'Prospect demo and MDA website links verified'
              with path.open('w', newline='', encoding='utf-8') as f:
                  writer = csv.DictWriter(f, fieldnames=fields, extrasaction='ignore'); writer.writeheader(); writer.writerows(rows)
          PYSTATUS

      - name: Save Batch 15 completion record
        shell: bash
        run: |
          set -euo pipefail
          git config user.name "mda-growth-systems"
          git config user.email "michael@mdagrowthsystems.info"
          git add -A
          git commit -m "Finalize Batch 15 status records [skip ci]" || true
          for attempt in 1 2 3 4; do
            git pull --rebase origin main
            if git push origin main; then
              exit 0
            fi
            sleep 4
          done
          exit 1
