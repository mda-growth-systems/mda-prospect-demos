document.querySelectorAll('[data-menu]').forEach(btn=>btn.addEventListener('click',()=>document.querySelector('.navlinks').classList.toggle('open')));
document.querySelectorAll('a[href^="#"]').forEach(a=>a.addEventListener('click',()=>document.querySelector('.navlinks')?.classList.remove('open')));
document.querySelectorAll('form[data-concept-form]').forEach(form=>form.addEventListener('submit',e=>{e.preventDefault();const result=form.querySelector('.form-result');result.style.display='block';result.textContent='Concept brief completed locally. No information has been transmitted. Open the confirmation page to continue the prototype.';const button=form.querySelector('button[type="submit"]');button.textContent='Open confirmation';button.type='button';button.onclick=()=>location.href='thank-you.html';result.scrollIntoView({behavior:'smooth',block:'center'});}));
