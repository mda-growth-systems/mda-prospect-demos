const menu=document.querySelector('.menu');const nav=document.querySelector('.navlinks');if(menu){menu.addEventListener('click',()=>{const open=nav.classList.toggle('open');menu.setAttribute('aria-expanded',String(open));});}
const form=document.getElementById('briefForm');const summary=document.getElementById('liveSummary');if(form){form.addEventListener('input',()=>{const vals=[...form.querySelectorAll('select,input')].filter(el=>el.value&& !['contactName','contactEmail','contactPhone'].includes(el.id)).slice(0,4).map(el=>{const label=form.querySelector(`label[for="${el.id}"]`);return `${label?label.textContent:'Detail'}: ${el.value}`});summary.textContent=vals.length?vals.join(' • '):'Choose the project details to build a concise brief.';});form.addEventListener('submit',e=>{e.preventDefault();if(!form.checkValidity()){form.reportValidity();return;}window.location.href='thank-you.html';});}