const menu=document.querySelector('.menu');const nav=document.querySelector('.navlinks');if(menu){menu.addEventListener('click',()=>{const open=nav.classList.toggle('open');menu.setAttribute('aria-expanded',String(open));});}document.querySelectorAll('.navlinks a').forEach(a=>a.addEventListener('click',()=>nav&&nav.classList.remove('open')));const form=document.getElementById('briefForm');if(form){form.addEventListener('submit',e=>{e.preventDefault();if(!form.checkValidity()){form.reportValidity();return;}const data=new FormData(form);const items=[...data.entries()].map(([k,v])=>`<li><strong>${k.replaceAll('_',' ')}:</strong> ${String(v).replace(/[<>]/g,'')}</li>`).join('');const preview=document.getElementById('preview');preview.innerHTML=`<strong>Prototype brief summary</strong><ul>${items}</ul><span class="small">Nothing has been transmitted or stored. Opening the confirmation page…</span>`;preview.style.display='block';setTimeout(()=>location.href='thank-you.html',900);});}